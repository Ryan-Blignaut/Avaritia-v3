/*
package thesilverecho.avaritia.client.render_layer;

import net.minecraft.client.renderer.RenderType;
import thesilverecho.avaritia.client.shader.Shader;

public class WrappedShaderLayer extends RenderType
{
	private final RenderType parent;
	private final Shader program;

	public WrappedShaderLayer(RenderType parent, Shader program)
	{
		super("", parent.format(), parent.mode(), parent.bufferSize(), parent.affectsCrumbling(), true, null, null);
		this.parent = parent;
		this.program = program;
	}


}
*/
